<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loading... | Digital Garden</title>
    
    <!-- Typography Imports -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&family=Playfair+Display:ital,wght@0,400;0,600;0,800;1,400&display=swap" rel="stylesheet">

    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";
        import { getFirestore, collection, doc, getDoc } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js";

        const firebaseConfig = typeof __firebase_config !== 'undefined' ? JSON.parse(__firebase_config) : {};
        const app = initializeApp(firebaseConfig);
        const db = getFirestore(app);
        const appId = typeof __app_id !== 'undefined' ? __app_id : 'default-app-id';
        const postsCol = collection(db, 'artifacts', appId, 'public', 'data', 'posts');

        async function loadArticle() {
            // Fix: Retrieve the ID from the URL parameters before checking it
            const urlParams = new URLSearchParams(window.location.search);
            const id = urlParams.get('id');

            if (!id) return;

            try {
                const docSnap = await getDoc(doc(postsCol, id));
                
                if (docSnap.exists()) {
                    const data = docSnap.data();
                    
                    // Update Meta
                    document.title = data.title + " | Digital Garden";
                    document.getElementById('art-date').innerText = new Date(data.date).toLocaleDateString().toUpperCase();
                    document.getElementById('art-tag').innerText = data.tag || '#NOTE';
                    document.getElementById('art-time').innerText = data.readTime || '3 MIN READ';
                    
                    // HANDLE ATTACHMENT (Generic)
                    // Supports new 'attachmentUrl' OR legacy 'imageUrl'
                    const fileUrl = data.attachmentUrl || data.imageUrl;
                    const fileType = data.attachmentType || ""; 
                    const fileName = data.attachmentName || "Attached File";

                    const attachContainer = document.getElementById('art-attachment-container');
                    attachContainer.innerHTML = ""; // Clear previous

                    if (fileUrl) {
                        // Case A: It's an Image
                        if (fileType.startsWith('image/') || fileUrl.match(/\.(jpeg|jpg|gif|png)$/i)) {
                             attachContainer.innerHTML = `<img src="${fileUrl}" alt="${data.title}" style="width:100%; height:auto; border:1px solid var(--ink); margin-bottom:2rem; display:block;">`;
                        } 
                        // Case B: It's another file type (PDF, Zip, etc)
                        else {
                            attachContainer.innerHTML = `
                                <div class="file-card">
                                    <div class="mono" style="margin-bottom:0.5rem; opacity:0.6;">FILE ATTACHMENT</div>
                                    <div style="font-family:var(--font-serif); font-size:1.2rem; margin-bottom:1rem;">${fileName}</div>
                                    <a href="${fileUrl}" target="_blank" class="file-btn">DOWNLOAD / VIEW →</a>
                                </div>
                            `;
                        }
                    }
                    
                    // Update Content
                    document.getElementById('art-title').innerText = data.title;
                    document.getElementById('art-intro').innerText = data.intro || '';
                    document.getElementById('art-content').innerHTML = data.content;
                } else {
                    window.location.href = '404.html';
                }
            } catch (e) {
                console.error("Error loading article:", e);
            }
        }

        window.addEventListener('DOMContentLoaded', loadArticle);
    </script>

    <style>
        /* --- SHARED DESIGN SYSTEM (Identical to Homepage) --- */
        :root {
            --paper: #F4F4F0;
            --ink: #1A1A1A;
            --ink-dim: #4A4A4A;
            --font-serif: 'Playfair Display', serif;
            --font-sans: 'Inter', sans-serif;
            --font-mono: 'JetBrains Mono', monospace;
            
            --spacing-xs: 0.5rem;
            --spacing-sm: 1rem;
            --spacing-md: 2rem;
            --spacing-lg: 4rem;
            --spacing-xl: 8rem;

            --border-width: 1px;
        }

        *, *::before, *::after {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: var(--paper);
            color: var(--ink);
            font-family: var(--font-sans);
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
            overflow-x: hidden;
        }

        /* --- NOISE TEXTURE --- */
        .noise-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            pointer-events: none;
            z-index: 9999;
            opacity: 0.4;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
            mix-blend-mode: overlay;
        }

        /* --- UTILITIES --- */
        a { color: inherit; text-decoration: none; }
        .container { max-width: 1400px; margin: 0 auto; padding: 0 var(--spacing-md); }
        .mono { font-family: var(--font-mono); text-transform: uppercase; font-size: 0.75rem; letter-spacing: 0.05em; }
        
        /* --- NAVIGATION --- */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: var(--spacing-md) 0;
            font-family: var(--font-mono);
            font-size: 0.85rem;
            border-bottom: var(--border-width) solid var(--ink);
        }

        /* --- READING PROGRESS BAR --- */
        .progress-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: transparent;
            z-index: 1000;
        }
        .progress-bar {
            height: 100%;
            background: var(--ink);
            width: 0%;
        }

        /* --- ARTICLE HEADER --- */
        .article-header {
            padding: var(--spacing-lg) 0 var(--spacing-md) 0;
            text-align: center;
            max-width: 900px;
            margin: 0 auto;
        }

        .article-meta {
            display: flex;
            justify-content: center;
            gap: var(--spacing-md);
            margin-bottom: var(--spacing-md);
            opacity: 0.7;
        }

        h1.article-title {
            font-family: var(--font-serif);
            font-size: clamp(2.5rem, 6vw, 5rem);
            line-height: 1.1;
            margin-bottom: var(--spacing-md);
        }

        .article-intro {
            font-size: 1.25rem;
            opacity: 0.8;
            font-style: italic;
            font-family: var(--font-serif);
            max-width: 700px;
            margin: 0 auto;
        }

        /* --- CONTENT TYPOGRAPHY (PROSE) --- */
        .content-wrapper {
            max-width: 720px; /* Optimal reading width */
            margin: 0 auto;
            padding-bottom: var(--spacing-xl);
        }

        .prose p {
            margin-bottom: 1.5rem;
            font-size: 1.1rem;
            line-height: 1.7;
        }

        .prose h2 {
            font-family: var(--font-serif);
            font-size: 2rem;
            margin-top: 3rem;
            margin-bottom: 1.5rem;
            border-bottom: 1px solid var(--ink);
            padding-bottom: 0.5rem;
        }

        .prose h3 {
            font-family: var(--font-mono);
            font-size: 1.1rem;
            margin-top: 2.5rem;
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        /* Blockquote Styling */
        .prose blockquote {
            border-left: 4px solid var(--ink);
            padding-left: var(--spacing-md);
            margin: 2.5rem 0;
            font-family: var(--font-serif);
            font-size: 1.5rem;
            font-style: italic;
            color: var(--ink-dim);
        }

        /* Code Block Styling */
        .prose pre {
            background: #e8e8e4;
            padding: 1.5rem;
            border: 1px solid var(--ink);
            overflow-x: auto;
            margin: 2rem 0;
            font-family: var(--font-mono);
            font-size: 0.9rem;
        }

        .prose code {
            font-family: var(--font-mono);
            background: #e8e8e4;
            padding: 0.2em 0.4em;
            font-size: 0.85em;
        }

        .prose pre code {
            background: transparent;
            padding: 0;
        }

        .prose ul {
            margin-bottom: 1.5rem;
            padding-left: 1.5rem;
        }
        
        .prose li {
            margin-bottom: 0.5rem;
        }

        /* --- NEXT POST NAV --- */
        .read-next {
            border-top: var(--border-width) solid var(--ink);
            padding: var(--spacing-lg) 0;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: var(--spacing-md);
        }

        .next-card {
            padding: var(--spacing-md);
            border: 1px solid transparent;
            transition: border 0.3s ease;
        }

        .next-card:hover {
            border: 1px solid var(--ink);
        }

        .next-label {
            display: block;
            margin-bottom: var(--spacing-xs);
            opacity: 0.5;
        }

        .next-title {
            font-family: var(--font-serif);
            font-size: 1.5rem;
        }

        /* --- FOOTER (Simplified) --- */
        footer {
            padding: var(--spacing-md) 0;
            text-align: center;
            border-top: var(--border-width) solid var(--ink);
            font-family: var(--font-mono);
            font-size: 0.8rem;
            opacity: 0.6;
        }
        
        /* New Styles for Non-Image Attachments */
        .file-card {
            border: 1px solid var(--ink);
            padding: 2rem;
            margin-bottom: 2rem;
            background: rgba(26,26,26, 0.02);
            text-align: center;
        }
        
        .file-btn {
            display: inline-block;
            padding: 0.5rem 1rem;
            border: 1px solid var(--ink);
            font-family: var(--font-mono);
            font-size: 0.8rem;
            text-transform: uppercase;
            transition: all 0.3s ease;
        }
        
        .file-btn:hover {
            background: var(--ink);
            color: var(--paper);
        }

        @media (max-width: 768px) {
            .read-next { grid-template-columns: 1fr; }
            h1.article-title { font-size: 2.5rem; }
        }
    </style>
</head>
<body>
    <div class="noise-overlay"></div>
    
    <!-- Reading Progress -->
    <div class="progress-container">
        <div class="progress-bar" id="progressBar"></div>
    </div>

    <div class="container">
        <nav>
            <div class="logo"><a href="index.html">DG — 2025</a></div>
            <div class="links">
                <a href="index.html">← BACK TO INDEX</a>
            </div>
        </nav>

        <article>
            <header class="article-header">
                <!-- ... existing header content ... -->
                 <div class="article-meta mono">
                    <span id="art-date">Nov 22, 2025</span>
                    <span>•</span>
                    <span id="art-tag">#DESIGN</span>
                    <span>•</span>
                    <span id="art-time">4 MIN READ</span>
                </div>
                <h1 class="article-title" id="art-title">The Architecture of Thought</h1>
                <p class="article-intro" id="art-intro">Exploring how we map digital spaces to mimic cognitive functions, and why the folder structure is obsolete.</p>
            </header>
            
            <!-- Replaced ID 'art-image-container' with 'art-attachment-container' -->
            <div class="content-wrapper">
                <div id="art-attachment-container"></div>
                <div class="prose" id="art-content">
                    <!-- Content injects here -->
                </div>
            </div>
        </article>

        <footer>
            © 2025 DIGITAL GARDEN
        </footer>
    </div>

    <script>
        // Reading Progress Bar Script
        window.onscroll = function() {
            let winScroll = document.body.scrollTop || document.documentElement.scrollTop;
            let height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            let scrolled = (winScroll / height) * 100;
            document.getElementById("progressBar").style.width = scrolled + "%";
        };
    </script>
</body>
</html>